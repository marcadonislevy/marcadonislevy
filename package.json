{
  "name": "marcadonislevy-profile",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "generate": "node scripts/generate-profile.mjs",
    "verify": "node scripts/verify-public-output.mjs",
    "preview": "node scripts/generate-profile.mjs --fixture test/fixture-stats.json --output preview-output"
  }
}
